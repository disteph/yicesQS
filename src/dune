(executable
 (name main)
 (libraries containers zarith yices2_bindings)
 (flags (:standard -cclib -lyices))
 (modes exe)
 (promote (until-clean) (into ".."))
 (preprocess (pps ppx_optcomp ppx_deriving.std))
)

(env (dev (flags (:standard -warn-error -A))))
